upstream backend {
    server ${TARGET}:5000; 
}

server {
    listen 80;
    location / {
        proxy_pass http://backend;
    }
}

server {
    listen 5000;
    location /status {
        stub_status;
    }
}